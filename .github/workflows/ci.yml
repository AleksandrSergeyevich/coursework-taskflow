name: ProdBoost — Docker Build Only

on: [push, pull_request]

jobs:
  docker-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker images
        run: docker compose build
      - name: Report success
        run: echo "✅ Docker images built successfully"
