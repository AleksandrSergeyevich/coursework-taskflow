# Используем официальный легковесный образ Nginx
FROM nginx:alpine

# Копируем все файлы фронтенда в директорию, откуда Nginx раздаёт статику
COPY . /usr/share/nginx/html

# Порт 80 уже открыт в базовом образе — дополнительно указывать не обязательно
# EXPOSE 80

# Запускаем Nginx в foreground-режиме (чтобы контейнер не завершался)
CMD ["nginx", "-g", "daemon off;"]
